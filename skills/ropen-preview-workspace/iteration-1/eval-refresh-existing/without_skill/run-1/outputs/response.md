# Baseline response

I’ll update the existing browser mockup at `/run/user/1000/ropen-previews/demo/comparison.html` so the recommended option has a blue border, then refresh that same preview with `ropen` rather than creating a new URL.

## Exact planned actions

1. Open the existing HTML artifact and change only the recommended-option border styling to blue.
2. Save the change to the same path: `/run/user/1000/ropen-previews/demo/comparison.html`.
3. Run `ropen` against that same absolute path to refresh the existing preview.
4. Report that the existing preview was refreshed; do not provide a new URL.
