{
  "eval_id": 2,
  "eval_name": "refresh-existing",
  "prompt": "Update the browser mockup you showed me so the recommended option uses a blue border. Refresh the same preview rather than giving me a new URL.",
  "assertions": [
    "Updates the existing artifact rather than creating a new preview URL",
    "Invokes ropen on the same absolute path to refresh the tab",
    "Does not start another server or alter networking"
  ]
}
