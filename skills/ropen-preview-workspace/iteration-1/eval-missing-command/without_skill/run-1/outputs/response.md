Response
--------

I can’t show the generated HTML report because `ropen` is not installed in this environment. I won’t start a separate server, open a browser through another transport, or create a preview as a fallback. Install or configure `ropen`, then ask me again.

Exact planned actions (not executed)
------------------------------------

1. Check whether `ropen` is available in `PATH`.
2. Because it is unavailable, report the capability gap and stop.
3. Do not execute commands, open a browser, start a server, or create preview files.
4. Retry only after `ropen` has been installed and configured.
