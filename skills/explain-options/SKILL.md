---
name: explain-options
description: Render or update explain-tts options. Bare invocation renders values; key=value arguments persist validated updates.
---
Run the shared `../explain-tts/bin/tts-options` in the foreground. Bare invocation renders current values. For trailing `key=value` arguments, validate and persist them atomically. Do not synthesize audio.
